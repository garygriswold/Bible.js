{
  "name": "web-proxy",
  "description": "An example of how to customise HTTP response codes and content types",
  "private": true,
  "files": [
    "*.js"
  ],
  "scripts": {
    "start": "claudia create --name web-proxy --region us-west-2 --api-module webproxy",
    "deploy": "claudia update"
  },
  "devDependencies": {
    "claudia": "^1.x"
  },
  "dependencies": {
    "claudia-api-builder": "^1.x",
    "superb": "^1.2.0"
  }
}
